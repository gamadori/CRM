@page "/Deals"
@page "/Deals/Index"

@using Radzen
@using Radzen.Blazor
@using CRM.Shared.Helper
@using CRM.Client.Shared.Filters



<BlazorHeader Header="@_header"></BlazorHeader>




@if (_deals != null)
{
    @if (IdProject == null && IdCompany == null)
    {
        <BlazorBreadCrumb Model=@_bread></BlazorBreadCrumb>
    }
    <BlazorFilter OnChangeFilter="OnChangeFilter" @bind-FilterState=@_filterState>
        <Filter>
            <div class="container m-3">
                <div class="form-floating">

                    <RadzenDropDownDataGrid class="form-control" @bind-value="@_idUser" TValue="string" AllowFiltering="true" FilterCaseSensitivity="FilterCaseSensitivity.CaseInsensitive" AllowClear="true" Change="OnChangeIdUser"
                                        Data="@_users" Count="@_users.Count" TextProperty="NameComplete" ValueProperty="Id">
                        <Columns>
                            <RadzenDropDownDataGridColumn Property="@nameof(ApplicationUser.Surname)" Title="@Localize[nameof(ApplicationUser.Surname)]" Width="auto" />
                            <RadzenDropDownDataGridColumn Property="@nameof(ApplicationUser.Name)" Title="@Localize[nameof(ApplicationUser.Name)]" Width="auto" />

                        </Columns>
                    </RadzenDropDownDataGrid>

                    <label>@Localize["Owner:"]</label>

                </div>
            </div>
        </Filter>
    </BlazorFilter>
    <div class=row>
        <div class="col">
            <BlazorButton Template="TemplateButton.New" Label=true OnClick="NewTicket"></BlazorButton>
        
        </div>
    </div>

    <RadzenDataGrid  @ref="grdDeals" Responsive=true IsLoading="@_isLoading" Count="@_deals.Items.Count" Data="@_deals.Items" LoadData="@LoadData" FilterCaseSensitivity="FilterCaseSensitivity.CaseInsensitive" FilterMode="FilterMode.Advanced" 
        AllowSorting="true" AllowFiltering="true" AllowPaging="true" 
        PageSize="10" PagerHorizontalAlign="Radzen.HorizontalAlign.Center" TItem="DealModel" EmptyText=@Localize["Nessun Deals"]>
        <Columns>


            <RadzenDataGridColumn TItem="DealModel" Property="@nameof(DealModel.Name)" Title="@TranslateHelper.GetDisplayName(nameof(Deal.Name), typeof(Deal))" Width="10%" />

            <RadzenDataGridColumn TItem="DealModel" Property="@nameof(DealModel.Date)" FormatString="{0:d}" Title="@TranslateHelper.GetDisplayName(nameof(Deal.Date), typeof(Deal))" Width="10%"/>


            <RadzenDataGridColumn TItem="DealModel" Property="@nameof(DealModel.Company)" Title="@TranslateHelper.GetDisplayName(nameof(Deal.IdCompany), typeof(Deal))" Type="typeof(string)"
                                FilterProperty="Company.RagioneSociale" SortProperty="Company.RagioneSociale"  Width="20%"  />

            <RadzenDataGridColumn TItem="DealModel" Property="@nameof(DealModel.Contact)" Title="@TranslateHelper.GetDisplayName(nameof(Deal.Contact), typeof(Deal))" Type="typeof(string)"
                                FilterProperty="Product.Name" Width="10%" />

            
            <RadzenDataGridColumn Context="item" TItem="DealModel" Property="@nameof(DealModel.State)" Title="@TranslateHelper.GetDisplayName(nameof(Deal.State), typeof(Deal))" Filterable="true" Sortable="true" TextAlign="Radzen.TextAlign.Center" Width="10%">
                <Template Context="item">
                    <span style="mix-blend-mode: difference; color: white">
                            @EnumService.Get(typeof(DealStates), item.State)
                        </span>
                </Template>
                
            </RadzenDataGridColumn>
            <RadzenDataGridColumn Context="item" TItem="DealModel" Property="@nameof(DealModel.Phase)" Title="@TranslateHelper.GetDisplayName(nameof(Deal.Phase), typeof(Deal))" Filterable="true" Sortable="true" TextAlign="Radzen.TextAlign.Center" 
                Width="10%">
                <Template Context="item">
                    <span style="mix-blend-mode: difference; color: white">

                        @EnumService.Get(typeof(DealPhases), item.Phase)
                    </span>
                </Template>

            </RadzenDataGridColumn>

            <RadzenDataGridColumn TItem="DealModel" Context="item" FormatString="{0:###,##0.00}" Property="@nameof(DealModel.Amount)" Title="@TranslateHelper.GetDisplayName(nameof(Deal.Amount), typeof(Deal))" Width="10%">
            
                <FooterTemplate>
                        @Localize["Total Amount"]: <b> @_deals.Total.ToString("###,##0.00")</b>
                </FooterTemplate>
            </RadzenDataGridColumn>

            <RadzenDataGridColumn Context="item" TItem="DealModel" Filterable="false" Sortable="false" TextAlign="Radzen.TextAlign.Center">
                <Template Context="item">
                        <RadzenButton ButtonStyle="Radzen.ButtonStyle.Secondary" Icon="format_list_bulleted" title="Dettagli"  Size="Radzen.ButtonSize.Medium" Click="@(args => Details(item.Id))">
                        </RadzenButton>
                        <RadzenButton ButtonStyle="Radzen.ButtonStyle.Primary" Icon="edit" Size="Radzen.ButtonSize.Medium" title="Edit" Disabled=@(!item.Permits.Edit()) Click="@(args => Edit(item.Id))">
                        </RadzenButton>
                        <RadzenButton ButtonStyle="Radzen.ButtonStyle.Danger" Icon="delete" Size="Radzen.ButtonSize.Medium" title="Elimina" Disabled=@(!item.Permits.Delete()) Click="@(args => Delete(item.Id))">
                        </RadzenButton>
                </Template>
           
            </RadzenDataGridColumn>
       
        </Columns>
    </RadzenDataGrid>
}
else
{
    <Waiting></Waiting>
}

    
    